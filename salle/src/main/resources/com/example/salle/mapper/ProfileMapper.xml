<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.salle.mapper.ProfileMapper">
    
    <select id="getSellerProductList" resultType="com.example.salle.domain.Product">
    	SELECT *
    	FROM product
    	WHERE PR_EMAIL = #{email};
    </select>
    
    <select id="getTotalProduct" resultType="int">
    	SELECT COUNT(*)
    	FROM product
    	WHERE PR_EMAIL = #{email};
    </select>
    
</mapper>